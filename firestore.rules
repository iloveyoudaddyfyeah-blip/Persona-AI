/**
 * @fileoverview Firestore Security Rules for the application.
 *
 * Core Philosophy:
 * This ruleset enforces a strict user-ownership model.  Each user can only
 * access their own data.  Schema validation is relaxed to allow for rapid prototyping.
 *
 * Data Structure:
 * - /users/{userId}: Stores user-specific data and settings.  The {userId}
 *   must match the authenticated user's UID.
 * - /users/{userId}/characters/{characterId}: Stores character profiles created or
 *   customized by the user. Each character is owned by the parent user.
 * - /users/{userId}/personas/{personaId}: Stores user-created personas.
 *
 * Key Security Decisions:
 * - Users can only access their own user document and associated subcollections.
 * - Data validation is minimized to essential checks for authorization.
 * - Listing of user documents is disallowed for privacy.
 *
 * Denormalization for Authorization:
 *  - The `id` field within the UserPersona and CharacterProfile entities is used to validate
 *    ownership on create operations, ensuring the document ID matches the authenticated user's UID.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * @description Root-level rule: All requests must originate from an authenticated user.
     * @path /databases/{database}/documents
     * @allow (read, write): if isSignedIn()
     * @deny (read, write): if !isSignedIn()
     * @principle Requires all requests to be authenticated.
     */
    match /{document=**} {
      allow read, write: if isSignedIn();
    }

    /**
     * @description Rules for the /users collection. Only the user can read/write their own document.
     * @path /users/{userId}
     * @allow (get): if isOwner(userId)
     * @allow (create): if isOwner(userId)
     * @allow (update): if isExistingOwner(userId)
     * @allow (delete): if isExistingOwner(userId)
     * @deny (list): Always deny listing of all user documents.
     * @allow (list): if false;
     * @deny (get, create, update, delete): if !isOwner(userId)
     * @principle Enforces user-ownership model.
     */
    match /users/{userId} {
      allow get: if isOwner(userId);
      allow list: if false;
      allow create: if isOwner(userId);
      allow update: if isExistingOwner(userId);
      allow delete: if isExistingOwner(userId);
    }

    /**
     * @description Rules for the /users/{userId}/characters collection. Only the user can read/write their own characters.
     * @path /users/{userId}/characters/{characterId}
     * @allow (get, list): if isOwner(userId)
     * @allow (create): if isOwner(userId)
     * @allow (update): if isExistingOwner(userId)
     * @allow (delete): if isExistingOwner(userId)
     * @deny (get, create, update, delete): if !isOwner(userId)
     * @principle Enforces user-ownership model.
     */
    match /users/{userId}/characters/{characterId} {
      allow get, list: if isOwner(userId);
      allow create: if isOwner(userId);
      allow update: if isExistingOwner(userId);
      allow delete: if isExistingOwner(userId);
    }

    /**
     * @description Rules for the /users/{userId}/personas collection. Only the user can read/write their own personas.
     * @path /users/{userId}/personas/{personaId}
     * @allow (get, list): if isOwner(userId)
     * @allow (create): if isOwner(userId)
     * @allow (update): if isExistingOwner(userId)
     * @allow (delete): if isExistingOwner(userId)
     * @deny (get, create, update, delete): if !isOwner(userId)
     * @principle Enforces user-ownership model.
     */
    match /users/{userId}/personas/{personaId} {
      allow get, list: if isOwner(userId);
      allow create: if isOwner(userId);
      allow update: if isExistingOwner(userId);
      allow delete: if isExistingOwner(userId);
    }

    // Helper functions
    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return request.auth.uid == userId;
    }

    function isExistingOwner(userId) {
        return isOwner(userId) && resource != null;
    }
  }
}